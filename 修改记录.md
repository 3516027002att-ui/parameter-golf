# 修改记录

## 2026-03-22 17:36:57 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\.venv\`
  - `D:\Repository\parameter-golf\data\datasets\fineweb10B_sp1024\fineweb_train_000000.bin`
  - `D:\Repository\parameter-golf\data\datasets\fineweb10B_sp1024\fineweb_val_000000.bin`
  - `D:\Repository\parameter-golf\data\tokenizers\fineweb_1024_bpe.model`
  - `D:\Repository\parameter-golf\data\tokenizers\fineweb_1024_bpe.vocab`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求执行本项目的第一步落地操作。
  - 建立独立 Python 虚拟环境，避免污染全局解释器。
  - 仅安装下载数据所需的最小依赖 `huggingface-hub`，不提前引入完整训练依赖。
  - 下载 `sp1024` 变体的最小 smoke 数据集：`1` 个训练分片、全量验证分片和 tokenizer 文件。

- 验证命令与结果：
  - `python -m venv .venv`
    - 结果：成功创建虚拟环境。
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe -m pip install --upgrade pip huggingface-hub`
    - 结果：成功；`pip` 升级到 `26.0.1`，`huggingface-hub` 安装完成。
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe data\cached_challenge_fineweb.py --variant sp1024 --train-shards 1`
    - 结果：成功下载；HF Hub 提示未设置 `HF_TOKEN`，以及 Windows 未启用符号链接导致缓存退化，但不影响本次下载完成。
  - `Get-ChildItem data\datasets\fineweb10B_sp1024 | Select-Object Name,Length,LastWriteTime`
    - 结果：
      - `fineweb_train_000000.bin` = `200001024` bytes
      - `fineweb_val_000000.bin` = `124044716` bytes
  - `Get-ChildItem data\tokenizers | Select-Object Name,Length,LastWriteTime`
    - 结果：
      - `fineweb_1024_bpe.model` = `254483` bytes
      - `fineweb_1024_bpe.vocab` = `9856` bytes

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：只做了项目第一步所需的最小充分变更，没有绕过校验、没有硬编码临时逻辑、没有复制平行机制，也没有提前安装无关的重型训练依赖。

- 回滚思路：
  - 删除本地虚拟环境：`Remove-Item -Recurse -Force .venv`
  - 删除已下载 smoke 数据：`Remove-Item -Recurse -Force data\datasets\fineweb10B_sp1024`
  - 删除 tokenizer：`Remove-Item -Force data\tokenizers\fineweb_1024_bpe.model, data\tokenizers\fineweb_1024_bpe.vocab`

## 2026-03-22 17:41:25 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\.venv\`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求继续完成完整训练依赖安装。
  - 直接复用仓库 `requirements.txt`，补齐 `torch`、`datasets`、`tiktoken`、`sentencepiece` 等训练和数据处理依赖。
  - 安装后执行统一导入验证，确认环境已经可供后续阅读代码、做 CPU 级 smoke 验证或转向远程 GPU。

- 验证命令与结果：
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe -m pip install -r D:\Repository\parameter-golf\requirements.txt`
    - 结果：成功，完整依赖安装完成。
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe -c "import numpy, torch, huggingface_hub, datasets, tiktoken, sentencepiece, kernels; print('imports_ok'); print('torch', torch.__version__); print('cuda_available', torch.cuda.is_available())"`
    - 结果：
      - `imports_ok`
      - `torch 2.10.0+cpu`
      - `cuda_available False`

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：严格沿用仓库现有依赖清单，没有手工拼装平行环境；安装后立即做导入验证，明确记录了当前仅为 CPU 版 `torch` 的事实，没有把“可导入”误报成“可正式训练”。

- 回滚思路：
  - 删除本地虚拟环境：`Remove-Item -Recurse -Force .venv`
## 2026-03-22 18:33:41 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\train_gpt.py`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求，为 `train_gpt.py` 添加详细中文注释。
  - 补充了模块总览、类和函数说明、参数与返回值解释、训练主流程说明、量化导出说明和关键变量用途说明。
  - 保持训练逻辑、接口和数值路径不变，只提升代码可读性与可维护性。

- 验证命令与结果：
  - `(Get-Content -Path 'train_gpt.py').Count`
    - 结果：`1287`
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe -m py_compile train_gpt.py`
    - 结果：通过，无语法错误
  - `git diff --stat -- train_gpt.py`
    - 结果：`318` 行新增，均为注释和 docstring

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：没有复制逻辑、没有引入临时分支、没有放宽校验，也没有改变模块边界；只是把现有实现的意图和结构解释清楚。

- 回滚思路：
  - 回滚本次全部改动：
    - `git checkout -- train_gpt.py 修改记录.md`
- 仅回滚注释文件：
     - `git checkout -- train_gpt.py`

## 2026-03-22 19:xx:xx +08:00

- 修改文件/路径：
  - `D:\claude code\memory\about_me.md` (长期记忆文件)

- 修改原因：
  - 根据对话内容，更新用户的长期记忆。
  - 记录当前正在做的项目：OpenAI Parameter Golf 挑战赛。
  - 记录探索方向：Int4 量化 + Outlier Isolation、架构改进、Tokenizer 优化。

- 关键文件阅读记录（未修改）：
  - `README.md` - 项目概览、规则、排行榜
  - `train_gpt.py` - CUDA 训练脚本（baseline）
  - `train_gpt_mlx.py` - MLX 训练脚本（Mac）
  - `records/track_10min_16mb/2026-03-20_10L_Int5MLP_MuonWD04_SWA50/README.md` - 当前 SOTA
  - `records/track_10min_16mb/2026-03-20_10L_Int5MLP_MuonWD04_SWA50/train_gpt.py` - SOTA 实现代码

- 项目进度分析：
  - **当前 SOTA**: val_bpb = 1.1428
  - **SOTA 关键技术**:
    1. 混合 Int5/Int6 量化（MLP 用 int5，Attention 用 int6）
    2. SmearGate + BigramHash(10240)
    3. Muon 优化器 + Weight Decay=0.04
    4. SWA (Stochastic Weight Averaging)
    5. Sliding Window Eval (stride=64)
  - **探索改进方向**:
    - 架构：Depth Recurrence、参数共享
    - 量化：Int4 + Outlier Isolation、Group Quantization
    - Tokenizer：更优词汇表、byte-level BPE
  - **关键结论**:
    - Baseline (9L, 512dim) 量化后约 12-14MB，符合 16MB 限制
    - Int4 可能显著降低性能，需配合 Outlier Isolation 或 QAT

- 验证命令与结果：
  - 阅读分析，无需验证命令

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅做阅读分析和记忆更新，未修改项目代码。

- 下一步计划：
  - 实现 Int4 + Outlier Isolation 量化方案
  - 或先复现 SOTA 代码做消融实验
  - 或申请 OpenAI 计算资源 ($1M credits)

## 2026-03-22 18:33:41 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\train_gpt.py`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求，为 `train_gpt.py` 添加详细中文注释。
  - 补充了模块总览、类和函数说明、参数与返回值解释、训练主流程说明、量化导出说明和关键变量用途说明。
  - 保持训练逻辑、接口和数值路径不变，只提升代码可读性与可维护性。

- 验证命令与结果：
  - `(Get-Content -Path 'train_gpt.py').Count`
    - 结果：`1287`
  - `D:\Repository\parameter-golf\.venv\Scripts\python.exe -m py_compile train_gpt.py`
    - 结果：通过，无语法错误
  - `git diff --stat -- train_gpt.py`
    - 结果：`318` 行新增，均为注释和 docstring

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：没有复制逻辑、没有引入临时分支、没有放宽校验，也没有改变模块边界；只是把现有实现的意图和结构解释清楚。

- 回滚思路：
  - 回滚本次全部改动：
    - `git checkout -- train_gpt.py 修改记录.md`
  - 仅回滚注释文件：
    - `git checkout -- train_gpt.py`

## 2026-03-22 21:50:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\train_gpt.py`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 实现 Depth Recurrence + Outlier Isolation + STE Fake-Quant/QAT 三合一改造。
  - **Depth Recurrence**：将 9 个独立 Block + U-Net skip 结构改为 2 shared blocks × 4 loops = 8 effective layers。移除 `skip_weights`、`num_encoder_layers`、`num_decoder_layers`，新增 `shared_blocks`（ModuleList[2]）和 `loop_scales`（Parameter[4,2,dim]）作为 per-loop scale adapter。
  - **Outlier Isolation**：导出侧新增 `isolate_outlier_cols` 函数，对 shared block 内的 2D 矩阵做 column-level sidecar quantization（top-16 列存 fp16，其余走 per-row int8）。
  - **STE Fake-Quant/QAT**：新增 `_FakeQuantSTE` autograd Function，在 `CastedLinear.forward` 中对 shared block 的权重做训练时 fake-quant。
  - 同步更新：Hyperparameters、CONTROL_TENSOR_NAME_PATTERNS、optimizer setup、quantize/dequantize 格式。

- 验证命令与结果：
  - `python -c "import ast; ..."` → syntax OK
  - `wc -l train_gpt.py` → 1476 行（限制 1500）
  - 无残留旧引用（skip_weights/.blocks./num_encoder/num_decoder）

- 技术债判断：
  - 本次没有新增技术债。Depth Recurrence 替换而非叠加旧结构；OI 与 per-row int8 正交互补；QAT 通过开关控制不侵入默认行为；量化格式向后兼容。

- 回滚思路：
  - `git checkout -- train_gpt.py 修改记录.md`

## 2026-03-23 xx:xx:xx +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\experiments.md`（新建）
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求，新建 `experiments.md` 用于记录各版本训练 plan 的对比。
  - 内容包括：两套训练方案（train_gpt.py 原版 vs plan1.py）的架构差异说明、Plan1 的具体改动点、历史全部实验的 val_bpb / 模型大小 / 关键配置汇总表、Plan1 待填写的结果表、改进趋势总结。
  - 数据来源：各实验目录的 submission.json 和修改记录.md。

- 验证命令与结果：
  - 文件创建成功，内容与历史实验数据一致

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅新建文档文件，未修改任何代码逻辑。

- 回滚思路：
  - 删除文件：`Remove-Item experiments.md`

## 2026-03-23 xx:xx:xx +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\CLAUDE.md`（新建）
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求，新建项目级 `CLAUDE.md`，为后续对话提供引导和约束。
  - 内容包括：项目概述、仓库结构说明、硬约束（16MB/10分钟/评估规则等）、工作流约束（修改前/中/后检查项）、实验运行说明、关键技术栈、改进方向优先级。
  - 信息来源：`README.md` 竞赛规则、`修改记录.md` 历史改动、`experiments.md` 实验数据、代码实际结构。

- 验证命令与结果：
  - 文件创建成功

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅新建文档文件，未修改任何代码逻辑。

- 回滚思路：
  - 删除文件：`Remove-Item CLAUDE.md`

## 2026-03-23 17:10:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\CLAUDE.md`
  - `D:\Repository\parameter-golf\experiments.md`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 修正 CLAUDE.md 中关于本地 GPU 的错误描述：原写"本地无 GPU（torch CPU only）"，实际有 RTX 4060 Laptop (8GB VRAM)，CUDA 12.7。
  - 在 experiments.md 中添加训练环境说明，明确所有历史实验均为本地 GPU 训练。

- 验证命令与结果：
  - `nvidia-smi` → RTX 4060 Laptop, 8GB VRAM, CUDA 12.7, Driver 566.07

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅修正文档中的错误描述，未修改任何代码逻辑。

- 回滚思路：
  - `git checkout -- CLAUDE.md experiments.md 修改记录.md`

## 2026-03-23 19:15:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\train_gpt.py`
  - `D:\Repository\parameter-golf\plan1.py`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 为 Windows 本地 GPU 训练添加特化支持：
    1. **GQA 兼容性**：移除 `enable_gqa` 参数（Windows + PyTorch 2.10 不支持），改为手动展开 KV heads
    2. **SDPA 后端切换**：Windows 上禁用 flash SDPA，改用 math 后端
    3. **禁用 torch.compile**：Windows 上 Triton 不可用，跳过编译优化
  - 同时修改 `train_gpt.py` 和 `plan1.py`，确保两套方案都能本地训练

- 验证命令与结果：
  - `.venv/Scripts/python.exe -m py_compile train_gpt.py` → 通过
  - `.venv/Scripts/python.exe -m py_compile plan1.py` → 通过
  - `nvidia-smi` → 训练运行中，GPU 100% 利用率

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：使用 `sys.platform != "win32"` 条件分支，仅在 Windows 上回退到兼容模式，不影响 Linux 生产环境。

- 回滚思路：
  - `git checkout -- train_gpt.py plan1.py 修改记录.md`

## 2026-03-25 10:00:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\plan2.py`（多次配置调整）
  - `D:\Repository\parameter-golf\LESSONS_LEARNED.md`（新建）
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - plan1 和 plan2 训练效果均变差，记录踩坑分析和改进建议。
  - plan1 主因：模型容量被砍爆（depth recurrence + QAT），解决不存在的问题。
  - plan2 主因：同时改太多变量，无法归因（seq_len、batch、LR 同时变化）。

- 踩坑总结（详见 LESSONS_LEARNED.md）：
  1. 不要提前为 size 付训练税，baseline 已在 16MB 内
  2. 不要把不同 seq_len 的日志直接比较
  3. 不要把 LR=0.02 当普适真理，它是 WD bundle 的一部分
  4. 不要做全程 QAT，收益抵不过开销
  5. 不要先上 10240 BigramHash，先用小桶验证
  6. 不要忽视 eval-only 增益（sliding eval）

- 推荐改进路径：
  1. plan3a：fp16 tok_emb + 保持 9 层 + 不改 seq_len/batch + warmdown 重调
  2. plan3b：Muon WD bundle（0.02 LR / 0.99 momentum / long warmup / clip=0.3）
  3. plan3c：SmearGate + BigramHash(2048/4096) + OrthoInit + 3x MLP
  4. plan3d：mixed int6/int5 + 10th layer + late QAT/EMA/GPTQ-lite

- 验证命令与结果：
  - plan2 多次训练，因显存不足多次 OOM，最终用 batch=32768, seq_len=128 跑到 11700 步后效果差
  - 日志分析：plan1 容量 1/4，plan2 变量混杂

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅记录踩坑经验，未修改核心训练逻辑。

- 回滚思路：
  - 删除文件：`Remove-Item LESSONS_LEARNED.md`
  - 恢复 plan2.py：`git checkout -- plan2.py`

## 2026-03-25 12:30:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\LESSONS_LEARNED.md`

- 修改原因：
  - 根据更深入的分析，更新踩坑记录：
  - 增加 plan1 失败的详细原因排序（容量砍太狠 > 改太多变量 > QAT 太早太重 > LR 未配平 > loop_scales 补偿不足）
  - 增加 recurrence 方向"未验证能赢"的证据（PR #38, #298 都比 baseline 差）
  - 增加"后处理优先于训练期噪声"的原则
  - 增加 public SOTA 路线对比表
  - 修正部分建议的强度标注（如 LR 缩放公式来源弱）
  - 增加 plan2 变量混杂问题的独立分析

- 关键更新点：
  1. **保留**：容量砍太狠、不要改太多变量、QAT 太早太重、便宜增益建议、后处理优先
  2. **弱化**：U-Net skip 必要性（有争议）、LR 缩放公式（来源 PR #38 本身不是强记录）
  3. **补充**：recurrence 方向整体未验证能赢、plan2 的 seq_len/batch 变量混杂问题

- 验证命令与结果：
  - 文档更新，无需验证命令

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅更新文档，记录分析结论。

- 回滚思路：
  - `git checkout -- LESSONS_LEARNED.md`

## 2026-03-25 19:33:02 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\plan3a.py`
  - `D:\Repository\parameter-golf\plan3b.py`
  - `D:\Repository\parameter-golf\plan3c.py`
  - `D:\Repository\parameter-golf\plan3d.py`
  - `D:\Repository\parameter-golf\run_plan3a.bat`
  - `D:\Repository\parameter-golf\run_plan3b.bat`
  - `D:\Repository\parameter-golf\run_plan3c.bat`
  - `D:\Repository\parameter-golf\run_plan3d.bat`
  - `D:\Repository\parameter-golf\experiments.md`
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按 Plan3a-d 独立实验方案新增四份独立训练脚本，保持 `train_gpt.py` 与 `train_gpt_origin.py` 本轮完全不动。
  - `plan3a.py` 加入 `tok_emb` fp16 passthrough、统一默认训练 regime、统一日志增强与 `quant_gap` 输出。
  - `plan3b.py` 在 `plan3a.py` 基础上加入 Muon WD bundle，并把 `muon_wd=0.04` 等固定参数直接固化为默认值。
  - `plan3c.py` 在 `plan3b.py` 基础上加入 3x MLP、SmearGate、BigramHash(4096)、OrthoInit，并保持 9 层不加第 10 层。
  - `plan3d.py` 在 `plan3c.py` 基础上加入 mixed int5/int6 导出与 roundtrip 校验，保持 `tok_emb` fp16 passthrough 和现有 `.ptz` 导出流程。
  - 新增最薄 launcher：`run_plan3a.bat` 到 `run_plan3d.bat`，仅负责调用对应脚本，不再重复覆写核心超参。
  - 更新 `experiments.md`，补充 Plan3a-d 实验矩阵、固定日志文件名和 launcher 对照关系。
  - 为避免技术债而做的必要最小重构：在四个 plan3 脚本内部补上 Windows 本地运行兼容修复，解决 smoke 中暴露的真实根因，而不是跳过验证：
    - 去掉 `enable_gqa=True`，改为手动展开 KV heads 后再走 SDPA。
    - Windows 下切换到 math SDPA，并跳过 `torch.compile`。
    - 修正 RoPE cache 在验证阶段以 inference tensor 形式缓存后，训练反向传播报错的问题，返回 `clone()` 后的常规张量。

- 验证命令与结果：
  - `python -m py_compile D:\Repository\parameter-golf\train_gpt.py D:\Repository\parameter-golf\plan3a.py D:\Repository\parameter-golf\plan3b.py D:\Repository\parameter-golf\plan3c.py D:\Repository\parameter-golf\plan3d.py`
    - 结果：通过。
  - `$env:DATA_PATH='D:\Repository\parameter-golf\.smoke_data\fineweb10B_sp1024'; $env:ITERATIONS='2'; $env:WARMUP_STEPS='0'; & 'D:\Repository\parameter-golf\.venv\Scripts\python.exe' 'D:\Repository\parameter-golf\plan3a.py'`
    - 结果：2-step smoke 通过；日志头出现 `iterations:2`、`max_wallclock_seconds:0.000`；训练行包含 `lr_scale`、`grad_norm`、`tokens`；完成 int8 导出与 roundtrip，`quant_gap:0.000000`。
  - `$env:DATA_PATH='D:\Repository\parameter-golf\.smoke_data\fineweb10B_sp1024'; $env:ITERATIONS='2'; $env:WARMUP_STEPS='0'; & 'D:\Repository\parameter-golf\.venv\Scripts\python.exe' 'D:\Repository\parameter-golf\plan3b.py'`
    - 结果：2-step smoke 通过；完成 int8 导出与 roundtrip，`quant_gap:-0.000000`。
  - `$env:DATA_PATH='D:\Repository\parameter-golf\.smoke_data\fineweb10B_sp1024'; $env:ITERATIONS='2'; $env:WARMUP_STEPS='0'; & 'D:\Repository\parameter-golf\.venv\Scripts\python.exe' 'D:\Repository\parameter-golf\plan3c.py'`
    - 结果：2-step smoke 通过；完成 int8 导出与 roundtrip，`quant_gap:0.000000`。
  - `$env:DATA_PATH='D:\Repository\parameter-golf\.smoke_data\fineweb10B_sp1024'; $env:ITERATIONS='2'; $env:WARMUP_STEPS='0'; & 'D:\Repository\parameter-golf\.venv\Scripts\python.exe' 'D:\Repository\parameter-golf\plan3d.py'`
    - 结果：2-step smoke 通过；完成 mixed int5/int6 导出与 roundtrip，`quant_gap:0.000000`。
  - 临时 smoke 数据说明：
    - 使用脚本从正式 shard 截取了极小验证集到 `D:\Repository\parameter-golf\.smoke_data\fineweb10B_sp1024`，仅用于快速启动与导出校验。
  - 清理状态：
    - 已尝试删除 `.smoke_data\`、`final_model.pt`、`final_model.int8.ptz`、`logs\plan3*_v1.txt` 与 `__pycache__`。
    - 结果：删除动作被当前工具策略拦截，临时产物仍在工作区，需后续手动清理。

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：
    - “四份独立脚本”是用户明确选择的实验管理方式，重复被限制在实验脚本层，没有反向污染 baseline 主线。
    - 没有通过关闭校验、放宽类型、吞异常或硬编码临时分支来换取表面通过。
    - Windows 兼容修改是 smoke 里真实暴露的运行时根因修复，不是绕过问题。
    - `plan3d` 只叠加 mixed int5/int6 这一量化轴，没有偷偷再混入第 10 层、late QAT、EMA 或 GPTQ-lite。

- 回滚思路：
  - 删除本次新增脚本与 launcher：
    - `Remove-Item D:\Repository\parameter-golf\plan3a.py, D:\Repository\parameter-golf\plan3b.py, D:\Repository\parameter-golf\plan3c.py, D:\Repository\parameter-golf\plan3d.py`
    - `Remove-Item D:\Repository\parameter-golf\run_plan3a.bat, D:\Repository\parameter-golf\run_plan3b.bat, D:\Repository\parameter-golf\run_plan3c.bat, D:\Repository\parameter-golf\run_plan3d.bat`
  - 恢复文档：
    - `git checkout -- D:\Repository\parameter-golf\experiments.md D:\Repository\parameter-golf\修改记录.md`
  - 清理本次 smoke 产物（若仍存在）：
    - `Remove-Item -Recurse -Force D:\Repository\parameter-golf\.smoke_data`
    - `Remove-Item -Force D:\Repository\parameter-golf\final_model.pt, D:\Repository\parameter-golf\final_model.int8.ptz`
    - `Remove-Item -Force D:\Repository\parameter-golf\logs\plan3a_v1.txt, D:\Repository\parameter-golf\logs\plan3b_v1.txt, D:\Repository\parameter-golf\logs\plan3c_v1.txt, D:\Repository\parameter-golf\logs\plan3d_v1.txt`

## 2026-03-25 20:00:00 +08:00

- 修改文件/路径：
  - `D:\Repository\parameter-golf\.context\`（新建目录结构）
  - `D:\Repository\parameter-golf\.context\system\project.md`（新建）
  - `D:\Repository\parameter-golf\.context\system\architecture.md`（新建）
  - `D:\Repository\parameter-golf\.context\memory\long-term.md`（新建）
  - `D:\Repository\parameter-golf\.context\memory\todo.md`（新建）
  - `D:\Repository\parameter-golf\.context\memory\decisions.md`（新建）
  - `D:\Repository\parameter-golf\.context\memory\handoff.md`（新建）
  - `D:\Repository\parameter-golf\.context\config.yaml`（新建）
  - `D:\Repository\parameter-golf\.context\main.md`（新建）
  - `D:\Repository\opencontext-central\contexts\project-index\projects.md`（更新）
  - `D:\Repository\parameter-golf\修改记录.md`

- 修改原因：
  - 按用户要求，为项目创建专属记忆系统 `.context/`。
  - 根据项目实际状态更新记忆内容：
    - 训练时间：无限制（`max_wallclock_seconds=0`），非 10 分钟
    - plan1/plan2：已验证失败，非"待验证"
    - 当前实验方向：plan3a-d 渐进式验证
    - 六条避坑原则已记录到 long-term.md
  - 将 parameter-golf 项目注册到中央层 `projects.md`。

- 验证命令与结果：
  - 文件创建与更新成功，无需额外验证命令

- 技术债判断：
  - 本次没有新增技术债。
  - 原因：仅创建文档和配置文件，未修改任何代码逻辑。

- 回滚思路：
  - 删除 `.context/` 目录：`Remove-Item -Recurse -Force .context`
  - 恢复中央层：`git checkout -- D:\Repository\opencontext-central\contexts\project-index\projects.md`
